# 構成式スキル Vertical Slice

> **REFERENCE — この領域の技術・実装資料。** ゲームの確定仕様の正本を置き換える文書ではありません。本文の旧ポリシー、WORK固有の指示、PR依存・SHA・検証結果はhistoricalな来歴で、現在の共通運用やGitHub状態には適用しません。[現行の文書案内](../README.md)。

## 2026-09-09 連続戦闘の反復調整

基準91b48b4から着手。develop cf6fee9を監査し、作業中にIntegrationから入った
待機姿勢テスト修正ae89fa4をfast-forwardで保持。PR #60の統合後、最新develop 713de66
（更新通知PR #61を含む）から後続branch work/combat-flow-20260909を作成。
通知UIの変更は保持。さらに提出中に入ったdevelop bd984a8の足運び・テンポ改善をWORKへ通常merge。
回転の新しい時間曲線と雷渡りの跳躍を共存させ、足運び・テンポ・構成・稽古帳の41テストと5描画ケースを再確認。
後続PRは #73。共通開発運用ポリシーv6を再確認。

- 稽古帳の「意識で連続戦闘」は本編の自動戦闘とphaseWeightsを使用。
  「技を閃く」は現在の技を中断せず、次の選択候補に加える。各段3候補・同じ重み。
  約1.1秒ごとの自動閃きも選択可能。位置、相手、負傷、スタミナは保持する。
  通常の単技試用は従来どおり。武器・相手・距離を変更すると新しい試行。
  繰り返しONでは連続戦闘を20秒ごとに再開。本編の習得にボタンや時間保証は追加しない。
- 本編の追跡が技の後退を打ち消していたため、技の間は技自身に移動を任せる。
  開始技は意識から一度選び、その技の間合いへ寄る。発動時に再抽選しない。
  既存の衝突、負傷、拘束、スタミナ、手動退避を通す。敵も通常どおり反撃する。
- 新規2技: 煤火の剣 / Cinderbrand (60093、破、片手剣)、
  雷渡り / Storm Swallow (60073、急、素手)。既存の炭・羽根の技族と経験交差から候補化。
  カタログは2,304構成＋42アクティブ＋5パッシブ。炎上・感電の状態異常は追加していない。
- 炎は実武器の根元と先端に沿って立ち上がり、火の粉が離れて残る。
  雷は実際の踏み込みから着地へ身体と同期し、移動した位置に短い電光を残す。
  後退・着地の砂ぼこりは描画中の足の接地点から発生。浮いた足や静止では作らない。
  火の粉8個/人、電光履歴6点/人、砂ぼこり全体32個、追跡48人を上限にし既存の演出予算内で描画。
- データ並べ替えや新技追加で構成技の基準が変わらないよう、技族の基準を最小の既存IDへ固定。
  公開済み2,304技の定義を維持。新しい不変server rulesを追加し、既存アーカイブを保持。

反復で直した点:
1. 初期位置へ毎回戻る試用を連続試用と分け、コンボ中の追跡割込みを除去。
2. 接地フレームと砂ぼこりを照合し、薄すぎた炎を太くして実武器の軌跡に結合。
3. 雷の溜め段階の値と跳躍時刻を修正。身体に隠れた電光を輪郭まで広げて再描画。

今回の検証:
- 関連126テストPASS。保存、client/server再生、入力・生活回帰、接続、足運び、武器、負傷、UIを含む。
  2種類の開始技の絶対時刻ではなく、接続対象技の実際の溜め時間を比較するよう接続テストを修正。
- 3相手×2seed×15秒を91b48b4と比較。コンボ中の追跡による移動は旧版19〜72フレーム、変更後は全6条件で0。
  かかし2条件の後退累計は5.10→8.34、3.98→6.25。移動量は大きいほどよいという評価ではない。
- 実Simulation＋実renderer処理（DOM / GL stub）で炎剣、雷渡り、後退と敵2種の12秒連続戦闘を確認。
  敵2種ではそれぞれ5・6種類の技が発動。行列・エフェクト頂点と描画値に非有限値なし。
  身体・装備・演出形状をnative EGLでフレーム描画し、修正後に再確認。
  この補助描画の照明・背景処理は簡略化されており、実ブラウザーの最終合成ではない。
- 実GLSL ESのworld / skin 2プログラムをnative EGLでcompile/link PASS。
- 同じ3見本の追加演出ON/OFFでCPU提出処理を比較。最終統合後ON中央値1.09〜1.68ms、p95 4.87〜7.79ms、
  OFF中央値0.83〜1.24ms。単回でばらつきがあり、性能向上や実機FPSの保証には使わない。
  詳細条件・比較数値は `combat-flow-check.json`。
- 確認HTML build PASS。ブラウザー／実機での連打操作、GPU負荷、戦闘の体感はUNVERIFIED。
  ローカルURLへのアクセス制約を迂回せず、Integration／ユーザーのHTML試遊へ引き継ぐ。

## 目的と範囲

人生から閃く技の候補を、既存の40アクティブ・5パッシブに加えて、
2,304個の安定したIDを持つ構成式アクティブ技へ拡張する。
色や微小な威力差を種類数には数えない。
12打撃形 × 4拍子 × 4足運び × 4命中作用 × 3終わり方。
「2,304個の専用アニメーション／完全に異なる見た目を手作業で作った」という意味ではない。
共有モーションを実際の打撃時刻と足運びに合わせ、軌道・打点・間合い・作用・終了状態を組み合わせる。

着手基準: develop c53f9b2。技名修正の提出前に develop 10fb501 へrebase済み（広場・敵図鑑・地形・成長・UIの更新を保持）。
今回の継続作業は共通開発運用ポリシーv6（2026-09-09確認）に従う。
develop 56588fe の関係箇所を監査した後、Integrationが同じWORKへ追加した
8c2b873（船・敵の状態表現・移動描画を統合）をfast-forwardで受け取り、そこへ通常の追加コミットを載せる。
履歴の書き換え・force push・PRのmergeは行わない。既存PR #60で受け渡す。

## 部品と接続

| 軸 | 内容 |
| --- | --- |
| 打ち方 | 針拳、炉拳、毬蹴、追枝、割拍、静掌、響打、羽輪、礎落、炭払、綾刃、結脚 |
| 拍子 | 一閃、等間隔の双返、時間差の間返、三綴 |
| 足運び | 据える、前進、横へ回る、引き足 |
| 命中作用 | 怯み、減速、弱体、押し離し |
| 終わり方 | 詰める、構えを整える、引く |

序破急の所属は構成から決まり、各ページ768個の候補を持つ。
前技が残す「近さ／拍子」と、次技が求める「近さ／拍子／崩れ」を
既存の接続判定に渡す。単に3技を置くだけで必ずボーナスが発生するわけではない。
崩れは既存技が残す状態にも接続できる。

`beatCuts` をSimulationの接触スケジュールとSkillMotionの拍子時計で共有。
既存キャラクターのクリップ再生もこの時計を使う。
前進・横移動・後退は既存の衝突処理を通す。負傷、拘束、年齢補正を適用。
回避・ガードされた正の威力の打撃は、状態作用・押し出し・接続を発生させない。
古い技の踏み込みタイミングと定義は維持する。

## 人生・習得・保存

- 既存の技族へ候補を加える。族の抽選重みを構成数で膨らませない。
- 候補の根拠が単一経験だけの族には、異なる種類の経験との交差を要求する。
- 小物は経験の由来として働く。血統は技族・経験への偏りとして働き、技はコピーしない。
- 習得済み技、所持品、武器、負傷、人生seed等の既存条件を利用する。
- アクティブの遅い習得ペースとパッシブの既存機会を保持。上限まで一生で覚える仕様ではない。
- 永続コードからIDとkeyを決定する。表示順や色seedでIDを変えない。
- 保存は既存schema4のID・編成・経験・閃き履歴を使い、復元で再抽選しない。
- 毎フレーム全候補を走査しない。経験イベントから索引で族を絞り、同じ条件の由来判定を機会内で共有する。
- ブラウザーとサーバーに同じ小さな構成定義を埋め込む。2,304定義の重複JSONは出荷しない。
- 既存の不変サーバールールは保持し、構成技導入・技名修正・後続developとの統合に対応するルールを追加する。

## 確認用HTML

### 技名と表示

- 技名は `names.ja` / `names.en` の対。英名は内部の部品keyの羅列にしない。
- 構成技は48組の「打ち方・拍子を思わせる名称」と48組の情景語を用意し、
  自然な和名・英名を組み立てる。例: **夕凪の待ち針 / Evening Calm Patient Needle**。
  2,304件それぞれを両言語で区別できる。情景語は技の呼び名であり、実際の習得経験を捏造するものではない。
- 一文字の部品コードや `〈崩繋〉` 等は技名へ入れない。序破急は別の編成表示。
- 意識UIは和名を一覧に、和名＋英名を詳細に表示。稽古帳も両名と編成する段を分けて表示。
- 呼び名は構成IDごとに固定し、人生・使用・ロードで変化させない。
  既存45件の名称、全技のID・key・挙動・習得条件・保存形式は変更しない。
- 名前データも不変サーバールールに含まれるため、新しいアーカイブを追加する。
  以前配布した名前を含む旧アーカイブは保持する。

`npm run build:skill-composition-lab`

出力: `dist/Bloodline_Legacy_Skill_Composition.html`

5軸の組み替え、6見本、一技／序破急、かかし・敵・複数相手、開始距離、
再生速度、カメラ、同条件リプレイ、構成JSON保存／読込。
本物のSimulationとSliceRendererを使う。稽古用に選んだ技を明示的に開放し、
本編のセーブやネットワークには接続しない。
横画面は右に操作、縦画面は下に操作を置く。

### 戦闘待機の構え（継続更新）

- 7f158dfからの描画変更。共通ポリシーv6を再確認し、develop ac9eb52の関係箇所を監査。
  本WORKの未統合の拍子時計・構成技を保持し、共有Simulationと不変ルールは変更しない。
- 自動戦闘・防御中と、直前に狙った相手への注視が続く間、腰を落とした構えへ移る。
  接地位置を保ち、重心・胸・腕・頭を小さく時間差で動かす。重い武器の揺れは抑える。
  構えの出入りを補間し、攻撃・被弾・休息・生活動作を優先。ヒットストップでは揺れの時計も停止。
- 新Travelerの4種族へ適用。攻撃クリップや歩行クリップを待機モーションで上書きしない。
  揺れの状態はrendererにだけ保持し、移動座標・当たり判定・戦闘seed・セーブを変更しない。
- 稽古帳の「構えを試す」で確認可能。初期のかかし相手なら攻撃せず構えだけを見られる。
- 関連14テストPASS: 4種族・複数武器の接地、描画だけの移動、ヒットストップ、
  待機から攻撃への接続、負傷・生活動作の優先、稽古帳操作を確認。
  `node tools/check-skill-composition-render.mjs --ready` で素手・片手剣・大剣の実描画経路を確認できる。
  GL stubとnative EGLでの確認であり、実ブラウザー／実機での目視は未検証。

### 足運びと試用機能

- 構成技の前進を0.85→1.9、横移動を0.6→1.25、引き足を0.45→0.9へ調整。
  終わりの追い足は0.35→0.8、離脱は0.65→1.35。単位はゲーム内距離。
  横移動中は相手へ滑らかに向き直り、壁・相手との間隔・拘束・負傷の既存制約を通す。
  名前・ID・習得頻度・威力・消費スタミナはこの調整で変えない。
- 稽古帳の初期カメラを固定。追従は任意で切替えられ、足運びを見比べやすくする。
  開始距離の手動指定に加えて、選んだ技の間合いに合わせる操作を追加。
- ランダムな一技の生成、直前の構成への復帰、序破急の一括抽選を追加。
  抽選は稽古帳専用seedを使い、本編の抽選や同条件試験のSimulation seedに干渉しない。
- 構成式2,304技は素手系。武器別2,304技を追加したという意味ではない。
  6武器の既存9技も試せるようにし、武器に合わない既存技の編成を防ぐ。
  構成技と武器技は混在編成でき、素手技の間は所持武器を背に預ける表示になる。
  装備状態や能力値は変更しない。専用の納刀アニメーションは未実装。
- 素手技が装備武器の剣クリップ・両手持ち補正・武器先端トレイルを借りないようにする。
  武器技は既存モデルと手の接続を使い、9技のエフェクトを武器先端と命中に結び付ける。
  専用モーションを全武器・全構成に制作したという意味ではない。
- 技に対応する演出の系統・軌道・命中・消え方を表示。演出ON/OFFで同じ戦闘を再生できる。
  個々の火花の見た目は描画側に閉じ、演出切替えで命中・移動結果を変えない。
- 稽古帳JSONをversion2へ拡張し、武器・抽選seedも保存。従来のversion1も読み込める。
  本編save schema4とは別。新しい複合サーバールールを追加し、公開済みアーカイブを保持。

## 検証

- 今回の8c2b873上の変更: `npm run build:skill-composition-lab` PASS。
  稽古帳、構成技、クリップ、負傷、船、敵の弱体動作、演出合成の関連7ファイル・49テストPASS。
  抽選・前の技へ戻る・全6武器の使用制約・演出ON/OFFでのSimulation一致・旧稽古帳JSON復元を含む。
  client/serverの戦闘途中セーブ再現、壁と相手の衝突、拘束中の足運びも確認。
- 16ケースすべてで実描画処理から演出が出力され、9武器技すべてで武器先端を取得。
  同じ実行から取り出した身体・装備の描画行列をnative EGLで描画し、手持ちと背の装備位置を目視確認。
  これは実ブラウザーの画面や最終エフェクト合成の確認ではない。
- 直前の8c2b873と同じ6見本・距離2.5・各180フレームで比較。
  更新後のCPU中央値は0.46–0.88ms、p95は0.87–2.00ms。
  `composition-preview-check.json` に前後の個別測定を記録。単回のDOM＋stub GL測定なので、
  GPU負荷・モバイルFPSや性能向上の保証として扱わない。
- 技名修正時点cabdc4aの `npm test`: 494件PASS。`node deploy/build.mjs dev` と `node deploy/check-build.mjs dev`: PASS（以前の結果）。
- GLSL ES 3.00 の12プログラムがnative EGLでコンパイル・リンクPASS。
- `node tools/check-skill-composition-render.mjs`: DOM上で6見本・9武器技・大剣を背に預けた素手技の実描画処理を実行。GL自体はstub。
  出力する姿勢データを既存native EGL描画ツールで確認。ブラウザー検証とは区別する。

- 構成2,304件のID一意性、定義検証、色・威力等を除いた挙動構造の一意性。
- 4拍子の接触時刻と姿勢時計、ヒットストップ、実移動、空振り・回避・ガード。
- 保存復元後の技ID／編成／次の閃き一致、血統が直接コピーしないこと。
- 構成技の戦闘途中セーブから、clientと不変serverルールで180tick進めた保存結果が完全一致。
- 48seed × 20分の同じ経験入力: authored版はアクティブ314／パッシブ207、
  拡張版は320／202。48人生すべて異なる習得構成。総技数の増加で習得ペースは急増しない。
- 実生活コマンドによる3人生・各12分。習得付与を使わず、すべてに経験理由が残る。
  `composition-life-results.json` に条件・習得・独立セーブからの戦闘比較を記録。
  「家族の鈴と手仕事」の同じ習得プールで、編成を変えると crawler 戦が14.47秒から7.27秒。
  学習済みプールからの別比較では敵種ごとに6編成を試し、勝敗も変化した。
  これは自動Simulation試験であり、人間のプレイ満足度を測ったものではない。
- warm Node CPUの候補抽出250標本: median 1.10ms / p95 7.49ms / p99 31.77ms。
  共有環境の計測であり、端末FPSや描画性能の保証ではない。

ブラウザー／実機の操作性・見た目・体感の最終確認は未実施。
既知のブラウザーアクセス制約を迂回せず、DOMと描画データ、GLSLコンパイルで確認する。
全構成の対人バランスや見分けやすさは保証しない。各部品の影響を確認できる稽古帳を添付する。
